# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '15.0'
inhibit_all_warnings!
use_frameworks!

target 'CastSender' do
  use_frameworks!
  
  # Pods for CastSender
  pod 'google-cast-sdk', '~> 4.8.3'
end
